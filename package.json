{
  "name": "comet_page_reader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test && vitest run tests/tokenizer.test.ts tests/textChunker.test.ts",
    "verify:npm-registry": "node scripts/verifyNpmRegistry.js",
    "build:extension": "node scripts/buildExtension.mjs",
    "build:extension:dev": "node scripts/buildExtension.mjs --mode development --sourcemap"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "vitest": "^1.6.1",
    "vite": "^5.4.21"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "http-proxy-agent": "^7.0.2",
    "https-proxy-agent": "^7.0.2",
    "yaml": "^2.8.1"
  }
}
